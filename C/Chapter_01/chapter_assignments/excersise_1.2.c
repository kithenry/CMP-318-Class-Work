#include <stdio.h>

// finding out what happens when print's argument contains \c
// where c is some arbitrary character apart from n.
//
// findings: escape sequences with various effects on formatting style.
// \t for tab, others not tried.
int main(){
	printf("what happens when \t is given as an argument\n");
}
